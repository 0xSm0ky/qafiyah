# Scripts Agent Guide

Repo tooling and CI checks, Bun/TypeScript plus bash wrappers. Every routine entry point is exposed as a `bun run <name>` script in the root `package.json`; use those names rather than paths so flags and working-directory handling stay in one place. `bun run test:scripts` and `bun run types:scripts` cover this directory (tests sit next to their module as `*.test.ts`). `scripts/ci.ts` is the whole quality gate (`bun run ci`, `--no-docker` to skip the Docker phases, `--docker-only` to run nothing else, `--phase <name>` for one phase); GitHub Actions runs it with `--no-docker` on every push and PR, and the Docker phases run only locally; `ci/phases.ts` is the phase selection those flags drive.

| Directory     | What lives here                                                                                                                                                                                                                                                                                                                                                                                                                    | `bun run` names                                                                                                                                                              |
| ------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `check/`      | repo-shape checks: cross-app import boundaries, constants kept in sync between `config.ts` and the Rust crates, file naming, parent-relative imports, the Rust toolchain pin and its keys, ShellCheck over every tracked shell script (found by `.sh` or shebang), hadolint over every Dockerfile, the SQL syntax check, the clippy wrapper behind `rust:lint`, and the opt-in commit identity guard the husky hooks call directly | `check:*`                                                                                                                                                                    |
| `dev/`        | the local stack: `run.ts` (`bun run dev`), the Compose wrapper with dev credential defaults, port preflight, db reset, dump-key management, worktree isolation, the `CLAUDE.md`/`GEMINI.md` symlinks, a rate-limit probe, the database-backed API test runner                                                                                                                                                                      | `dev`, `dev:preflight`, `db:up`, `db:reset`, `up`, `down`, `clean`, `dump:key:*`, `agents:link`, `quota`, `rust:test:db`                                                     |
| `db/`         | dump lifecycle: resolve and decrypt, restore (on first boot, and in place by `db:reseed`), split, encrypt, the fallback sample, before/after state diffs, and `sql/` (the refresh functions and the sample query)                                                                                                                                                                                                                  | `db:reseed`; the rest are mounted by Compose or called from `dev/` scripts and the `local-db-edit` skill, and the dump ones are documented in `data/db/MAINTAINERS_GUIDE.md` |
| `es/`         | Elasticsearch: reindex (dev and prod), live-index snapshot and diff, the ES query-builder drift snapshot                                                                                                                                                                                                                                                                                                                           | `reindex`, `reindex:prod`, `es:*`                                                                                                                                            |
| `openapi/`    | the OpenAPI document snapshot and the generated TypeScript client types                                                                                                                                                                                                                                                                                                                                                            | `openapi:*`                                                                                                                                                                  |
| `taxonomy/`   | regenerates the web app's taxonomy filter options from the live API                                                                                                                                                                                                                                                                                                                                                                | `taxonomy:generate*`                                                                                                                                                         |
| `well-known/` | renders `well-known/*` templates into the web app's generated module                                                                                                                                                                                                                                                                                                                                                               | `well-known:generate*`                                                                                                                                                       |
| `secrets/`    | SOPS/age wrappers and the secrets schema (`schema.ts` is the single list of allowed keys)                                                                                                                                                                                                                                                                                                                                          | `secrets:*`                                                                                                                                                                  |
| `smoke/`      | black-box HTTP probes against a dev or prod stack; any real poem or poet a probe names comes from `smoke/fixtures.ts`, which the fallback sample is built from                                                                                                                                                                                                                                                                     | `smoke:dev`, `smoke:prod`                                                                                                                                                    |
| `api/`        | replays every OpenAPI operation against a running API and validates the responses                                                                                                                                                                                                                                                                                                                                                  | `api:conformance`                                                                                                                                                            |
| `deploy/`     | the VPS deploy and the per-service image build used by the Images workflow                                                                                                                                                                                                                                                                                                                                                         | `deploy`, `build:images`                                                                                                                                                     |
| `ci/`         | phase selection for `ci.ts` (`--phase`, `--no-docker`, `--docker-only`)                                                                                                                                                                                                                                                                                                                                                            | `ci`                                                                                                                                                                         |
| `accounts/`   | the accounts-database backup and the systemd timer that runs it on the VPS                                                                                                                                                                                                                                                                                                                                                         | none (systemd timer, see `docs/deployment/services.md`)                                                                                                                      |
| `corpus/`     | offline classical-form validation of the poem corpus (`export-corpus.sh`, `verify-classical.ts`, `apply-verdicts.ts`); reads the dev database and writes under `reports/`, which is gitignored                                                                                                                                                                                                                                     | `corpus:*`                                                                                                                                                                   |
| `lib/`        | shared helpers: repo root, file walking, tracked-file listing, the external-linter runner, snapshot writing, cargo dumps, remote/VPS helpers, the db container tag                                                                                                                                                                                                                                                                 | none                                                                                                                                                                         |
